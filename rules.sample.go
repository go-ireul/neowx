package main

/*
import "ireul.com/neowx/types"

func init() {
	rules = []types.Rule{
		{
			Fn: func(m types.WxReq, cfg types.Config) bool {
				return m.ToUserName == cfg.DefaultAccount().OrignalID
			},
			Text: "Hello for DefaultAccount",
		},
	}
}
*/
